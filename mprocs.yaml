procs:
  supersim:
    cwd: .
    shell: pnpm supersim --interop.autorelay
  frontend:
    cwd: packages/frontend
    shell: pnpm dev
  deploy-contracts:
    cwd: packages/contracts
    shell: pnpm deploy:dev
